import SectionTitle from "../../../Components/SectionTitle";

const Payment = () => {
    return (
        <div>
            <SectionTitle heading={'Payment'} subHeading={'Chose your payment method'}></SectionTitle>
            
        </div>
    );
};

export default Payment;